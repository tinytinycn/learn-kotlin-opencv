
rootProject.name = "learn-kotlin-opencv"

pluginManagement{
    // gradle插件使用 aliyun maven gradle-plugin 镜像
    repositories {
        maven { setUrl("https://maven.aliyun.com/repository/gradle-plugin") }
    }
}

